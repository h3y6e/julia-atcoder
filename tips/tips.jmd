# Tips
## 標準入出力
### 1つの整数値
```text
N
```
```julia
N = parse(Int, readline())
```

### 複数の整数値
```text
N, M
```
```julia
N, M = map(x -> parse(Int, x), readline() |> split)
```

### 配列 (横)
```text
A_1 ... A_N
```
```julia
A = map(x -> parse(Int, x), readline() |> split)
```

### 配列 (縦)
```text
N
A_1
 :
A_N
```
```julia
N = parse(Int, readline())
A = zeros(Int, N)
for i = 1:N
  A[i] = parse(Int, readline())
end
```
